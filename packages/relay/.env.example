# Relay server configuration
PORT=3000

# Used to form full user handles (e.g. alice@connectclaw.io)
RELAY_HOST=localhost

# SQLite database path
DATABASE_URL=./data/connectclaw.db

# Leave empty for open relay, set a value to require access token for signup
RELAY_ACCESS_TOKEN=
